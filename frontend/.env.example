# create a .env.local file in the frontend directory and add the following environment variables:
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_APP_NAME=

# backend (future)
NEXT_PUBLIC_API_URL=

# MongoDB Connection
MONGODB_URI=

# JWT Authentication
JWT_SECRET=

# OAuth - Google (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# OAuth - GitHub (https://github.com/settings/developers)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Secret used to derive a per-user password for OAuth-created accounts (HMAC key).
# Must be a long random secret, set once, and never change once users exist -
# changing it will lock out existing OAuth users.
OAUTH_PASSWORD_SECRET=

# Demo/Testing bypass - access /chat?demo=YOUR_TOKEN to skip auth
DEMO_ACCESS_TOKEN=



